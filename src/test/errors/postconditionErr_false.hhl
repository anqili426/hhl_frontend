/*
    A simple test case for errors caused by a postcondition which may not hold.
*/

method postconditionError() returns (out: Int)
requires exists <_s> :: true
ensures exists <_s> :: _s[out] == 0
{
    out := 1
}
