var n: Int
var sum :Int
n := 0
sum := 1

while (n <= 100)
invariant forall _s: State :: get(_s, n) == $n && get(_s, sum) == $n + 1 && $n <= 101
{
        n := n + 1
        sum := sum + 1
}

ensures forall _s: State :: get(_s, sum) == 102
