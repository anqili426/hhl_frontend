method test1(x: Int, y: Int)
    ensures forall _s: State :: <_s> implies get(_s, x) == get(_s, y) && get(_s, x) == 1
{
    x := 1
    y := 1
}

method test2(x: Int, y: Int)
    ensures forall _s: State :: <_s> implies get(_s, x) == get(_s, y)
{
    x := y
    havoc x
}