method randBool() returns (y:Int)
    requires exists <_s> :: true
	ensures forall <_s> :: 0 <= _s[y] && _s[y] <= 1
	ensures exists <_s> :: _s[y] == 0
	ensures exists <_s> :: _s[y] == 1
{
	var x: Int
	havoc x {hint}
	use hint(0)
	use hint(1)
	if (x > 0) {
		y := 1
	}
	else {
		y := 0
	}
}
