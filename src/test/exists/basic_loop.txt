method test(x: Int) returns (y: Int)
requires exists _s: State:: <_s> && get(_s, x) == 2
ensures exists _s: State :: <_s> && get(_s, y) == 5
{
    y := 0
    let $s: State :: <$s> && get($s, x) == 2 && get($s, y) == 0
    while (y < 5)
    {hint} invariant $n <= 5 ==> exists _s: State :: <_s> && get(_s, y) <= 5 && get(_s, y) == $n
    {
        y := y + 1
    }
    use hint(5)
}